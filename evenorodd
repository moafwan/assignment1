#include <iostream>
using namespace std;

int main() {
  int number;
  cout << "Enter an integer number: ";
  cin >> number;

  if (number % 2 == 0) {
    cout << number << "Number is even." << endl;
  } else {
    cout << number << "Number is odd." << endl;
  }

  return 0;
}
